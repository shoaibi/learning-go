package main

func main() {
	// Bitwise Comparison
	// &, |
	// ^ = either one or the other but not both
	// &^ = Opposite of |, only if neither of them has bit set

	// Bitwise shift
	// 2^x, `<< x` multiple by 2^x
	// `>> x` divide by 2^x
}
